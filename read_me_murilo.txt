Para usar a aplicação ative o venv o comando "env\Scripts\activate" no diretorio raiz, 
rode "py -m pip install -r requirements.txt" para instalar dependências
e por último rode python models.py o output sera printando no console